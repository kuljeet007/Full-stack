import React from 'react'

export const CardShow = () => {
    return (
        <div>
            <Card
            key = {post.id}
            name = {post.name}
            body = {post.body}
        />
        </div>
    )
}